oneshots:
  startled:
    priority: 30
    duration_ms: 600
  confused:
    priority: 25
    duration_ms: 800
  welcome:
    priority: 20
    duration_ms: 1500
  happy:
    priority: 20
    duration_ms: 1000

scenes:
  startled_then_track:
    overlay: "assets/animations/startled.txt"
    sfx: ["startled"]
  welcome_back:
    overlay: "assets/animations/welcome.txt"
    sfx: ["welcome"]
  sleep_mode_loop:
    overlay: "assets/animations/sleep.txt"
    sfx: ["sleep"]
  take_photo_countdown:
    overlay: "assets/ui/camera_countdown.txt"
    sfx: ["camera_countdown"]
  smarthome_feedback:
    overlay: "assets/ui/smarthome_badge.txt"
    sfx: ["success"]
  petting_reaction:
    overlay: "assets/animations/petting.txt"
    sfx: ["happy"]
  default_scene:
    overlay: null

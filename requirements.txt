# Runtime dependencies for RIO on Raspberry Pi
# Install with: python3 -m pip install -r requirements.txt
# Local development/runtime was checked on Python 3.11.

# Configuration files
PyYAML>=6,<7

# Core math/image support used by the live interaction runner and vision stack
numpy>=1.24,<2
opencv-contrib-python>=4.10,<5

# Vision gesture / face detection
mediapipe==0.10.18

# Asset preparation and display tools
Pillow>=9.4,<13
pygame>=2.1,<3

# ── Live voice backend (torch-free) ───────────────────────────
# Pi (aarch64) 기준. 첫 실행 시 Whisper base 모델 ~145MB 자동 다운로드.
# 일반 텍스트/카메라 실행에는 없어도 되지만, live mic 음성 인식 실행에는 필요.
sounddevice>=0.5,<0.6
faster-whisper>=1.2,<2
